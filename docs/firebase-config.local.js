/**
 * Deployed stub so GitHub Pages does not 404 this script.
 * Optional overrides: edit in your clone only; avoid committing secrets.
 * See firebase-config.example.js for full config and Firebase setup notes.
 */
